services:
  postgres:
    image: postgres:18
    container_name: paywealth_postgres

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: n3u3d4!
      POSTGRES_DB: paywealth

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data:
